# Task Completion

A task is considered complete when:
1. Code formatting passes (`make fmt`).
2. Module dependencies are consistent (`make tidy`).
3. Linter passes with no errors (`make lint`).
4. The binary builds successfully (`make build`).